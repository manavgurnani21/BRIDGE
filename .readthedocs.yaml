version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

conda:
  environment: BRIDGE.yml

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements-rtd.txt