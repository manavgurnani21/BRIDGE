version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "miniforge3-latest"

conda:
  environment: BRIDGE.yml

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements-rtd.txt